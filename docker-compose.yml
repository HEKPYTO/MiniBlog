services:
  miniblog:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/miniblog.db
      - BETTER_AUTH_SECRET=changeme
      - ORIGIN=http://localhost:4321
    restart: unless-stopped
